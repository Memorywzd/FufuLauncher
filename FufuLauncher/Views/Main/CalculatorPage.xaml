<Page
    x:Class="FufuLauncher.Views.CalculatorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:FufuLauncher.ViewModels"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Storyboard x:Name="EntranceStoryboard">
            <DoubleAnimation Storyboard.TargetName="RootLayoutGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Name="ExitStoryboard">
            <DoubleAnimation Storyboard.TargetName="RootLayoutGrid"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <Grid x:Name="RootLayoutGrid" Padding="24" Opacity="0">
        <Grid.Transitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="40"/>
            </TransitionCollection>
        </Grid.Transitions>
        
        <Border x:Name="WebCardContainer"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                CornerRadius="16"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                Translation="0,0,32"
                SizeChanged="WebCardContainer_SizeChanged">
            
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Grid>
                <WebView2 x:Name="CalculatorWebView"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          DefaultBackgroundColor="Transparent"/>

                <ProgressBar x:Name="LoadingBar" 
                             IsIndeterminate="True" 
                             VerticalAlignment="Top" 
                             Height="3"
                             Background="Transparent"
                             Foreground="{ThemeResource SystemAccentColor}"
                             Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                             Canvas.ZIndex="100"/>
            </Grid>
        </Border>
    </Grid>
</Page>